<?php

use Illuminate\Foundation\Testing\RefreshDatabase;

uses(RefreshDatabase::class);

test('API is accessible', function () {
    $response = $this->get('/api/{{ moduleNameLower }}');
    $response->assertStatus(200);
});
